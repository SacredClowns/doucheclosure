# Cast photos

Drop a photo here named after the person, e.g. `gene-sticco.jpg`.
Then in `grifters.html`, find that person in the CAST list and set:

    img:"assets/cast/gene-sticco.jpg"

Square images look best (they're cropped to a circle in the cylinder).
Leave `img:null` and it falls back to their brass initial.
